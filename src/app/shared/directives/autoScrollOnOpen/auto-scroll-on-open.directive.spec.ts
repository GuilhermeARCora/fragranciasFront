import { AutoScrollOnOpenDirective } from './auto-scroll-on-open.directive';

describe('AutoScrollOnOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollOnOpenDirective();
    expect(directive).toBeTruthy();
  });
});
